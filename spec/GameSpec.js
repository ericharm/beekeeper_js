describe("Game", function () {
    var game = require("../src/game.js");
    var ctx = null;

    describe("game", function () {
        it("does something", function () {
            var Game = game.new(ctx);
        });  
    });

});
