describe("Player", function () {
    var Player = require("../src/player.js");

    beforeEach(function () {
        player = Player();
    });

    describe("an input event occurs in the game", function () {
        it("receives the event", function () {
            //
        }); 
    });

    describe("a command is created from an event", function () {
        it("passes it to the command queue in World", function () {
            //
        });
    });


});
