module.exports = {

    // should pass in delta and multiply
    setMoving: function (player, direction, value) {
        player.setMoving(direction, value);
    },
    // should pass in delta and multiply

    new: function (node, deltaTime) {
        
    }
};
